{{ form_start(form,{'attr': { 'class': 'field' }}) }}
    {{ form_row(form.email, {'attr': { 'class': 'field' }}) }}
    {{ form_row(form.password, {'attr': { 'class': 'field' }}) }}

    <button class="btn primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
